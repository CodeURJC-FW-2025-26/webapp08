<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <title>Error - ZenTao</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/Style.css">
</head>

<body>

  {{> header }}

  <div class="container mt-5">
    <div class="contact-card">

      <!-- main message sent from the controller -->
      <h2 class="text-danger mb-4">
        {{message}}
      </h2>

      <!-- list of errors (validation.errors) -->
      {{#errors}}
        <p>- {{msg}}</p>
      {{/errors}}

      <!-- button to return to the appropriate page -->
      <a href="{{backLink}}" class="btn btn-create">
        Volver
      </a>

    </div>
  </div>

  {{> footer }}

</body>
</html>

